#include "main-is_in_loadscreen.h"

/*
Handle user inputs
*/
void gamestateIsInLoadscreen::keyHandler(ASGE::SharedEventData data) {
	//Get key
	auto key = static_cast<const ASGE::KeyEvent*>(data.get());
}